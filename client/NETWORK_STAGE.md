# Network management slice — ТЗ v2.12

Status: working local file-view slice, not completion of the full TZ or production network authorization. No new server endpoints, database schema, grants, events, data storage, or production policy were introduced.

## Requirement → implementation → tests → remaining

Authoritative references below refer to `Fresh_Portal_v2.12_review/Fresh_Portal_TZ_v2.12_review.md`, with §45 overrides; decisions refer to `Fresh_Portal_v2.12_review/DECISIONS_AND_GATES.md` and `BUSINESS_RULES.md`. These private specification files are not copied into the public client.

| Exact requirement | Implementation / contract in this slice | Tests / evidence | Remaining |
| --- | --- | --- | --- |
| §10: metric + period + presentation + drill-down; §18: FRESH visual language; §24.5: mobile | Home is whole-network file view. Four network KPI retain official row 2 regardless of comparison filtering. Searchable branch-union table, four selectable additive ranks, ascending/descending, shared ranks for ties, missing-last, branch drawer that preserves network/filter context. Compact dark shell, pale focus panels; horizontal mobile table with branch name/header fixed. | `tests/networkModel.test.ts`: union/source precedence, missing-last in both directions, negative/zero values, ties/search position. Playwright 1440×1000 and 375×812; screenshots `FRESH_network_desktop_*`, `FRESH_network_mobile_*` outside repo. | Admin-configurable widgets, server snapshots, real network/division/RM scopes and approved metric catalogs. No branch-cabinet route added. |
| §45.1: real OrgUnits and current grants; COMDIR ≠ RM alias; §45.5: branch identity must not write into another branch | “Вся сеть · локальный файловый обзор”, “распределение по РМ не настроено”, no synthetic RM mapping. `rm_a` remains synthetic REGIONAL_MANAGER. File branch action explains the required OrgUnit/affiliation/responsible/grant mapping; it cannot call task creation. Header create action explicitly belongs to the task pilot. | Browser: imported-branch mapping notice creates no task; A task inaccessible to B; role changes clear imported data. API/auth/FSM source hashes unchanged. | Approved business identity map, effective-dated affiliation and real scope authorization at server/worker/export boundaries; D-01, G-DATA/G-MIG. |
| §9: explicit plan binding; §45.2: internal [start,end), display inclusive conversion; §33.4: no final comparison of short actual vs full-month plan | `salesPlan()` reads fact C and plan B **only from the second sales report**. User explicitly confirms independent actual and plan dates, displayed as inclusive dates. Ratio C/B×100 and nonnegative units remaining appear only for identical periods, non-null fact and positive plan. No duration calculation occurs; no internal time-series interval or server snapshot is represented. Unconfirmed dates hide plan. Zero plan / missing values / unequal periods block ratio with reason. | Unit tests: differing periods, unknown period, zero plan, missing fact, conflicting primary source. Browser: 10-day actual vs full-month plan has no ratio; equal-period source-derived ratios; all-nine actual-file check used deliberately unequal QA dates, not certification of the real sales period. | Approved plan_id/revision/approval/policy binding; actual source-period verification. No plan_to_date/seasonality model. The local comparison is not final financial achievement or an approved organizational plan. |
| §11.2.1–4, .6–9: computability, optional target, month-end evaluation, audit; §22.4.1–2: 1–5 unique active focus slots | Two working **comparison candidates**, not ACTIVE Monthly Focus: source sales/plan and 45+ share. Human-readable unavailability, checked N of M branches, and explicit filters for below-source-plan / above-local-target. Three other dependency areas remain visible, not silently hidden. | Unit + browser checks cover current-data availability, source scope, focus subsets, missing rows excluded rather than treated as success. | Full five-stage computability check needs metric registry, CONNECTED data source and previous-month trial; responsible assignments, 1–5 unique active slot editor, activation, field_history, archive, rollover, month-end job, Today/digest and AI precheck are **not implemented**. No API format or ACTIVE status is simulated. |
| §18.11.1 / .8: `hangers45_total`, `stock_units_end` from main summary; §45.7: ratios recalculated, quality independent of RAG | `stockShare()` = same-source AB/E×100, same validated stock date, denominator >0; aged cannot exceed stock. Network ratio is computed from official network components, **not mean or sum of branch percentages**. Label preserves the source's “45+” method; no invented age cutoff interpretation. | Unit: official ratio vs branch average, date absence, zero/missing/invalid count. Existing importer validates stock and 45+ dates match. Local real-file ratio checked against independent raw cells at displayed precision. | Approved metric/formula registry version, stock policy, source-date history. This factual descriptive ratio carries no default normative threshold. |
| §11.2.1: optional target/direction; §9 versioned plans; §45.9 no client policy permission | Optional **local draft** stock-share ceiling for the snapshot month. Explicit numeric 0–100% input, no seeded value; target month must match stock snapshot month. Revisions within the draft, edit/delete, percentage-point difference, subset filter. Draft resets on package replacement/reset/role/navigation/reload. | Unit range/empty/zero/month tests; browser out-of-range/month rejection, revision update, deletion, filtered rows, replacement clearing draft. | No persistence, server audit or approval; local revision is not immutable organization policy or field_history. No ACHIEVED/month-end classification, score, sanction or monetary effect. |
| §9 + D-05; §22.5.2 + §45.6: governed financial formulas; §45.7–9: no unsupported advisory | Margin plan/MBO held as HOLD_POLICY_REQUIRED. RunRate unavailable: TZ catalogs sales/margin forecasts but the explicit retrieved RunRate formula is for **royalty**, not a license to invent sales cutoff/formula/seasonality. §45.2 interval convention alone does not approve those models. No “85/100”, stock target, score, royalty or average turnover is invented. | UI dependency disclosure and unequal-period blocker; unit no ratio on unequal periods. | CFO/PO-approved source/plan revision, formulas/time anchor/cutoff and D-05, G-FIN. Real diary/discipline data, not synthetic A/B counts. |
| §45.5 atomic import and format limits; §45.3 truthful saving state | Existing worker/parser/limits/confirmation/atomic replacement unchanged. Imported data and targets stay in React memory; no storage APIs added to demo. Existing provenance/reconciliation retained; network tile source action resets reconciliation selector to official row. Reconciliation warnings visible separately from focus comparisons. | Original 10 synthetic importer tests + 10 new model tests pass. Browser duplicate-batch rejection, cancel during worker startup, replacement clearing missing sources, reset/navigation. All nine actual files locally validated without screenshots or business-data network traffic. | Production server probe/commit hashes, dataset permissions, server-side scan/validation/quarantine, persisted audit, cross-device/offline business cache are not implemented. |

## Files / architecture

- New: `src/imports/networkModel.ts`, `src/components/NetworkManagement.tsx`, `src/styles/network.css`, `tests/networkModel.test.ts`, this document.
- Changed: `src/components/LocalBusinessData.tsx`, `src/pages/DashboardPage.tsx`, `src/components/Layout.tsx` (stage label only; per-client theme behavior retained).
- Synchronized to `fresh-pplx-demo`, `freshdashboard/client`, `freshdashboard-public/client`. Demo retains HashRouter/demoFetch/in-memory theme; canonical/public retain BrowserRouter and server API/auth, including their existing theme storage.
- `reportModel.ts` already contained additional revenue/baseMargin/KSO fields at slice start. This slice does **not** extend or change report fields, headers, units, parser/worker, `localImport.test.ts`, task FSM, credentials or production API. Four existing additive metrics are intentionally the ranking scope.
- No legacy configurations, identities, secrets, accounts, business workbook values or synthetic workbook blobs are embedded in client bundles. Legacy dashboard/focus files used only as read-only visual reference.

## Verification and evidence

- All three clients: TypeScript + Vite build pass.
- Existing importer tests: 10/10. New network model tests: 10/10.
- `FRESH_network_QA_inventory.txt`, `FRESH_network_browser_results.json`, `FRESH_network_integrity_results.json`, `FRESH_network_*_build.txt`, `FRESH_network_import_results.txt`, `FRESH_network_model_results.txt` are workspace evidence, not deployed assets.
- Browser lifecycle smoke: create → assign → RF start → unsaved-result guard → save → submit → RM accept; out-of-scope B cannot read A task. Main file-view comparisons never write a task.
- Desktop and phone: synthetic import, ranking, search, focus filters, explicit target editing, source navigation, native drawers, no-match case, failed replacement, cancellation, reset and role/navigation clearing. No page errors or document-width overflow in inspected states. Internal table scrolling is intentional and labelled.
- Actual nine-file check: raw official totals, every displayed primary-source branch fact and network stock share matched an independent local XML extraction (currency rounding respected); source sales periods remain unknown and the test used deliberately mismatched QA dates. No real-data screenshots or values were emitted. Existing font assets load on initial app startup; after that, import generated no outbound/fetch/XHR traffic.

## Release boundary

This is a working next vertical slice, not full-TZ acceptance, a production Metric/Plan/Focus Engine, real commercial-director RBAC, or a no-code administration release. No deploy, publish, push or commit performed by the worker. Parent may preview the existing demo `dist`; public deployment remains a separate decision.
