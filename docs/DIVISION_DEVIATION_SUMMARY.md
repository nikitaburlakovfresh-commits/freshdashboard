# Сводка отклонений по дивизиону

Экран руководителя верхнего уровня замыкает управленческий цикл сверху: он
показывает, где именно в области ответственности находятся отклонения, по каким
из них задача ещё не поставлена и какие задачи вышли за срок.

## Что реализовано

### Сервер
- `GET /api/v1/metrics/divisions/deviations?start&end[&division]` —
  `server/src/metrics/divisionSummary.ts`. Новых таблиц и миграций не потребовалось:
  сводка считается по опубликованным срезам, настроенным порогам, задачам по
  отклонениям и действующим назначениям.
- Разрезы: по дивизионам, внутри дивизиона по региональным менеджерам, при
  раскрытии — по филиалам с перечнем красных и жёлтых показателей.
- Разбор по показателям: сколько филиалов дивизиона красные и жёлтые по каждому
  показателю, чтобы отличить проблему одного филиала от системной.
- Сроки задач считаются от настройки `deviation_task_due_soon_hours`, а не от
  зашитого значения; действующее окно возвращается в ответе.

### Принципиальные ограничения, заложенные в расчёт
- Сводка не обходит разграничение доступа: в неё попадают только филиалы и
  показатели, на которые у пользователя есть допуск READ. Отзыв допуска убирает
  филиал, а вместе с последним филиалом — и дивизион.
- Отсутствие опубликованного значения показывается отдельным признаком
  (`metrics_missing`, `branches_without_data`) и никогда не приравнивается к нулю
  или к выполнению плана.
- Филиал относится к дивизиону по подчинённости, действующей на дату конца
  периода, поэтому смена структуры не переписывает историческую сводку.
- Региональный менеджер определяется действующим назначением на филиал; вакансия
  показывается явно и не переносится на дивизион. При нескольких действующих
  назначениях берётся самое раннее по дате вступления в силу.
- Показатель без настроенного порога попадает в отдельный перечень: статус по
  нему не рассчитывается и не считается зелёным.

### Клиент
- Страница `/division-summary` «Сводка по дивизионам» в разделе меню
  «Управление результатом»: период, фильтр по дивизиону, итоги, разбор по
  показателям, таблица региональных менеджеров и раскрываемый перечень филиалов
  со ссылками на карточку филиала.
- Чистая модель `client/src/components/divisionSummaryModel.ts`: порядок фокуса,
  подпись вакансии, подпись отсутствующих данных.

## Постановка задачи прямо из сводки

Каждая строка филиала отдаёт перечень `open_deviations` — это и есть основание
постановки: показатель, статус, значение, версия снимка и версия порога.

- Кнопка «Поставить задачу» открывает ту же форму, что и карточка филиала
  (`DeviationTaskModal`), без дублирования логики.
- Клиентский статус основанием не является: сервер заново проверяет отклонение по
  текущим данным и порогам. Если показатель переопубликован, задача не создаётся
  и возвращается `DEVIATION_CONFLICT` с требованием обновить сводку.
- Повторная постановка по тому же основанию не создаёт вторую задачу.
- Право на филиал проверяется отдельно от права видеть сводку: строка может быть
  видна, а постановка задачи — запрещена (403).
- Если задача уже стоит, вместо кнопки выводится её состояние со ссылкой на карточку.

## Настраиваемая приоритетность риска

ТЗ v2.12 §1 требует сортировку по утверждённой приоритетности риска. Порядок
вывода больше не зашит в код: веса живут в настройках портала
(`024_risk_priority.sql`, реестр `SETTING_SPECS`).

| Настройка | Значение по умолчанию |
|---|---|
| `risk_weight_red` | 100 |
| `risk_weight_amber` | 40 |
| `risk_weight_deviation_without_task` | 25 |
| `risk_weight_task_overdue` | 60 |
| `risk_weight_branch_without_data` | 15 |

- Оценка риска — сумма произведений признаков на веса; она задаёт порядок
  дивизионов, региональных менеджеров и филиалов и выводится в ответе
  (`risk_score`, `risk_weights`), чтобы порядок можно было объяснить.
- Изменение веса требует основания 16–500 символов, пишется в аудит и в
  append-only историю, меняется без правки кода.
- Вес влияет только на порядок вывода. Ни один показатель, статус и ни одна
  задача от изменения веса не меняются — это проверено тестом.
- Экран сводки печатает действующие веса и отсылает к разделу настроек.

## Тесты
- `server/test/divisionSummary.ts` — 6 тестов:

| Тест | Проверяет |
|---|---|
| DS-01 | сессия, допуск к показателям, точный период, неизвестный дивизион → 404 |
| DS-02 | группировка по дивизионам, разбор по показателям, порядок по остроте |
| DS-03 | отсутствие публикации показано отдельно и не приравнено к нулю |
| DS-04 | отклонения без задачи отделены от задач, просрочка считается фактом |
| DS-05 | разрез по региональным менеджерам, вакансия названа явно |
| DS-06 | сводка не выходит за пределы допусков: отзыв допуска убирает дивизион |
| DS-07 | приоритетность риска задаётся настройкой портала: смена весов меняет порядок, показатели неизменны, история с основанием сохраняется |
| DS-08 | задача ставится из строки сводки по её основанию; без полномочий 403, повтор и устаревший снимок — `DEVIATION_CONFLICT` |

- `client/tests/divisionSummaryModel.test.ts` — 9 тестов: порядок фокуса, подпись
  вакансии, подпись отсутствующих данных, формула оценки риска, нулевой вес,
  приоритет «нет данных важнее цвета», подпись действия по отклонению, полнота
  названий весов.
- Регрессия: серверный прогон 139 тестов, изолированные наборы проверены
  (divisionSummary 8, portalSettings 5, metricDeviationTasks 24, reportBatches 40
  и остальные без изменений), клиент 76 тестов, типизация сервера и клиента
  чистая, сборка клиента успешна.

## Расхождения с ТЗ

- Правило 33.6.3 предполагает **автоматическую** задачу при RED два закрытых
  месяца подряд. Реализована только ручная постановка; автоправило не сделано.
- Правило 33.6.4 требует обязательного пояснения BH к каждой RED-строке. Не сделано.
- ТЗ §1 допускает **ручную корректировку** порядка дневного списка с указанием
  причины. Сейчас настраиваются веса приоритетности; ручная перестановка
  отдельной строки с причиной не реализована.

## Расхождения и ограничения
- Дивизиональные планы и целевые доли рынка в сводке не используются: таких
  подтверждённых данных в портале нет, а выдумывать план нельзя.
- Сравнение с предыдущим периодом не выводится: для него нужен согласованный
  перечень сопоставимых периодов по каждому показателю.
- Уровень кластера в разрезах не показывается отдельно, филиалы кластера
  попадают в свой дивизион.

## Границы
Изменения только в локальной ветке `feat/identity-access-v1`. В GitHub не отправлены,
production `72.56.32.190` и `freshdaily.ru` не затронуты.
